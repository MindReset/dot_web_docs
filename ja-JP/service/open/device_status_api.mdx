---
title: デバイスステータスを取得
---

import Link from 'fumadocs-core/link';
import { ApiPlayground } from 'src/shadcn/components/api-playground';
import { TypeTable } from 'src/shadcn/components/type-table';

## クイックガイド

### 必要条件

1. Dot. アプリで<Link href="/docs/service/studio/api/get_api">API キーを取得して保存</Link>していること
2. 自分のデバイスが少なくとも 1 台あり、<Link href="/docs/service/studio/api/get_device_id">デバイスのシリアル番号</Link>を取得していること

<Callout
	type="info"
	title="APIのリクエスト制限は1秒あたり10回です。APIを使うとデバイスの動作と応答をより柔軟に制御できます。"
/>

<Callout
	type="info"
	title="ドキュメント中の「{{変数}}」は置換が必要なプレースホルダーです。実際の値に置き換え、「{{}}」は削除してください。"
/>

### リクエスト例

<ApiPlayground
	baseUrl="https://dot.mindreset.tech"
	endpoint="/api/authV2/open/device/:id/status"
	method="GET"
	pathParams={{
		id: {
			type: 'string',
			required: true,
			name: 'デバイスのシリアル番号',
		},
	}}
	auth={true}
	defaultOpen={false}
/>

### レスポンス例

```json
{
	"deviceId": "ABCD1234ABCD",
	"alias": null,
	"location": null,
	"status": {
		"version": "1.0.0",
		"current": "電源アクティブ",
		"description": "デバイスは電源アクティブで、すぐに使用できます。",
		"battery": "充電中",
		"wifi": "-62 dBm"
	},
	"renderInfo": {
		"last": "12/18/2025 14:11",
		"current": {
			"rotated": false,
			"border": 0,
			"image": ["https://example.com/render/0.png", "https://example.com/render/1.png"]
		},
		"next": {
			"battery": "12/18/2025 17:11",
			"power": "12/18/2025 14:16"
		}
	}
}
```

## トラブルシューティング

### リクエストパス

`/api/authV2/open/device/:id/status`

### メソッド

`GET`

### パスパラメータ

<TypeTable
	type={{
		id: {
			description: 'デバイスのシリアル番号',
			type: 'string',
			required: true,
		},
	}}
/>

### レスポンス構造

<TypeTable
	type={{
		deviceId: {
			description: 'デバイスのシリアル番号',
			type: 'string',
			required: true,
		},
		alias: {
			description: 'デバイスの別名',
			type: 'string | null',
		},
		location: {
			description: 'デバイスの場所',
			type: 'string | null',
		},
		status: {
			description: 'デバイスのステータス情報',
			type: 'object',
			required: true,
			parameters: {
				version: {
					description: 'ソフトウェアバージョン',
					type: 'string',
					required: true,
				},
				current: {
					description: 'デバイスの現在のステータス',
					type: 'string',
					required: true,
				},
				description: {
					description: 'デバイスステータスの詳細な説明',
					type: 'string',
					required: true,
				},
				battery: {
					description: 'バッテリーステータス',
					type: 'string',
					required: true,
				},
				wifi: {
					description: 'WiFi 信号強度',
					type: 'string',
					required: true,
				},
			},
		},
		renderInfo: {
			description: 'レンダリング情報',
			type: 'object',
			required: true,
			parameters: {
				last: {
					description: 'デバイスが最後にレンダリングした時刻',
					type: 'string',
					required: true,
				},
				current: {
					description: '現在のレンダリング情報',
					type: 'object',
					required: true,
					parameters: {
						rotated: {
							description: '画面が回転しているかどうか',
							type: 'boolean',
							required: true,
						},
						border: {
							description: '画面のボーダーカラー（0=白、1=黒）',
							type: 'number',
							required: true,
						},
						image: {
							description: '現在のレンダリング画像 URL 一覧',
							type: 'string[] | null',
						},
					},
				},
				next: {
					description: '次回のスケジュールされたレンダリング時刻',
					type: 'object',
					required: true,
					parameters: {
						battery: {
							description: 'バッテリーモードでの次回レンダリング時刻',
							type: 'string',
							required: true,
						},
						power: {
							description: '電源モードでの次回レンダリング時刻',
							type: 'string',
							required: true,
						},
					},
				},
			},
		},
	}}
/>

### レスポンスコード

| code  | 意味           | 説明                           |
| :---- | :------------- | :----------------------------- |
| `200` | 成功           | デバイスステータスの取得成功   |
| `400` | パラメータ不正 | デバイスIDの形式エラー         |
| `401` | 未認証         | API キーが無効または期限切れ   |
| `403` | 権限不足       | このデバイスを表示する権限なし |
| `404` | 未検出         | デバイスが存在しない・未登録   |
| `500` | サーバーエラー | サーバー内部エラー             |
